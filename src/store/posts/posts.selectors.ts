export const selectorFetchingPostList = (state: any) =>
  state.posts.fetchingPostList;

export const selectorFetchingPostDetail = (state: any) =>
  state.posts.fetchingPostDetail;

export const selectorSelectedPost = (state: any) => state.posts.selectedPost;
