import React from "react";

interface IFiltroPaisesProps {
  onFilter: Function;
}

export const FiltroPaises = (props: IFiltroPaisesProps) => {
  return (
    <div>
      <input />
      <button>Filtrar</button>
    </div>
  );
};
